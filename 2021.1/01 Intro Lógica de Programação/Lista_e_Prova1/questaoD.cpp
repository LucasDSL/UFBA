#include <iostream>
using namespace std;
int main(){
    long a, b, c, d, e, soma = 0;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    cin >> e;
    
    soma += a;
    soma += b;
    soma += c;
    soma += d;
    soma += e;
    cout << soma << endl;
}