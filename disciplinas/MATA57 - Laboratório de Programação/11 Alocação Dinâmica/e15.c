#include<stdio.h>
#include<stdlib.h>

int main(){
    // Lendo os números de linhas e colunas da matriz
    int linhas, colunas;
    printf("Digite o numero de linhas da coluna:");
    scanf("%d", &linhas);
    printf("Digite o numero de colunas:");
    scanf("%d", &colunas);
}